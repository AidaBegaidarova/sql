server{
	listen 80;
	server_name stepik;
	

	location ^~ /uploads/{
		root /home/box/web/;
	}
	location ~* \.(gif|jpeg|jpg|js|css|img)$ {
		root /home/box/web/public/;
	}
	location / {
		return 404;
	}
}
